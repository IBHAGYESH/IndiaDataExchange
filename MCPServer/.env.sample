# India Data Exchange IDE API (same origin as marketplace backend)
IDE_API_BASE_URL=http://localhost:5001

# HTTP port for this MCP server (Streamable HTTP on POST/GET/DELETE /mcp)
PORT=5056

# Optional: require Authorization: Bearer <key> or X-MCP-API-Key: <key> on /mcp
# MCP_API_KEY=
